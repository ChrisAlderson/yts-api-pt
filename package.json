{
  "name": "yts-api-pt",
  "version": "2.0.0",
  "description": "A NodeJS wrapper for yts.ag",
  "dependencies": {
    "got": "^7.1.0"
  },
  "devDependencies": {
    "chai": "^3.1.1",
    "coveralls": "^2.13.1",
    "eslint": "^4.5.0",
    "eslint-config-vixo": "^2.0.0",
    "mocha": "^3.5.0",
    "nyc": "^11.1.0"
  },
  "license": "MIT",
  "scripts": {
    "coveralls": "cat ./coverage/lcov.info | node node_modules/coveralls/bin/coveralls",
    "lint": "./node_modules/.bin/eslint {examples,test}/** *.js",
    "test": "./node_modules/.bin/nyc --reporter=text --reporter=lcov ./node_modules/.bin/mocha --timeout 15000 --reporter spec test/**"
  },
  "engines": {
    "node": ">=6.3.0"
  },
  "keywords": [
    "yts",
    "movies",
    "api",
    "torrent"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ChrisAlderson/yts-api-pt.git"
  }
}
